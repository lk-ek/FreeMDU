# T4223C / software ID 498 (experimental)

Read-only device support, automatically selected by `device::connect`, used by
both TUI and Home Assistant. No full unlock, HALT or writes are issued.
Read key: `0x2b2c`. Full-key candidate `0x0f2f` received repeated HALT ACKs in
separate experiments; this does not verify arbitrary writes and is not used here.

## Observed map

| Address | Interpretation |
| --- | --- |
| 00b6 | Selector: 0f Ende, 0e Koch/Bunt Schranktrocken+, 08 Pflegeleicht Schranktrocken+, 09 Schranktrocken, 0d Schranktrocken/Schonen, 0c Buegelfeucht, 04 20 min warm, 05 15 min kalt |
| 0265..0267 | Door: 000000 closed, 010101 open; disagreement unknown |
| 0270 | aa active program, 55 inactive; not a finished flag |
| 027e | Suspected motor command: 0 off/pause, 1/2 direction A/B; not measured movement |
| 0260 | Transient post-run marker, raw only; returned from 1 to 0 |
| 026a / 027f | Raw flags/transition observations, no countdown interpretation |

Selector 09/0d/0c inferred from sequential physical knob sweep and panel labels.
Door validated by closed/open/closed tests without changing selector.
A cold run changed aa to 55 about 13m12s after first aa sample, without selector
or door change. During 12m18s of recorded post-run, brief alternating direction
markers occurred, initially about 70s apart. Anti-crease is plausible but not
independently verified. Opening during a run and manual abort were subsequently tested; see update below.
No reliable completion event, remaining time, temperature or fault decode exists.

## Integration

Operation properties use `dryer_*` identifiers, including program, door, run_state,
motion, phase and raw markers. Home reads three blocks once per publication and
shares them across all dryer properties; the existing publication interval remains
unchanged. Sequential reads are not atomic. TUI queries fresh snapshots.
No cached stale snapshot is returned after a read failure. Existing profiles are
unchanged; this patch does not add the fork-specific ID410 profile or dual IR ports.

Known readable windows: memory 0000..047f, EEPROM 0000..00ff (byte addressed).
Higher addresses timed out even with full access and could disrupt communication.
These are observed diagnostic limits, not verified physical memory sizes.
The driver reads only 00b0..00bf, 0260..026f and 0270..027f, with no EEPROM reads.

## Regression fixtures

Unmodified labelled 1152-byte dumps from the tested ID498 appliance, in
`protocol/tests/fixtures/id498`, ordered:

- 0.bin: ende-2
- 1.bin: koch-schranktrocken+-2
- 2.bin: pflegeleicht-schranktrocken+-2
- 3.bin: 20min-warm
- 4.bin: 15min-kalt
- 5.bin: 15min-kalt-tuer-auf
- 6.bin: 15min-kalt-tuer-zu-gestartet
- 7.bin: 20min-warm-tuer-zu-gestartet
- 8.bin: 20min-warm-tuer-zu-manuell-beendet

## Validation update, 2026-09-08

The annotated capture confirms: opening the door while running changes aa to 55;
closing it leaves 55 and the appliance waits for Start; pressing Start returns aa.
Turning the selector to Ende also changes aa to 55. Selecting cold again without
Start leaves 55. Consequently 55 is not a completion indication. Closed-door idle,
interrupted and post-run states are deliberately not distinguished without history.
An open door with 55 is shown as Door open / inactive; conflicting samples remain
Unknown. No inferred finished, paused-cycle or anti-crease event is emitted.

Additional annotated selector positions: 01 Koch/Bunt Schranktrocken/Schonen,
03 Koch/Bunt Buegelfeucht, 02 Mangelfeucht, 06 Glaetten, 07 Finish Wolle.
Koch/Bunt Schranktrocken WITHOUT plus was not captured unambiguously and remains
unmapped. Some reads took 6–12 seconds and overlapped selector changes; no mapping
is invented for the missing position. All three blocks are sequential, so an
individual snapshot may contain values from opposite sides of a transition.

Direction and anti-crease interpretations above remain research hypotheses.
The motion entity now reports Marker 0/1/2 only; its existing identifier is retained.
